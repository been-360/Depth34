pub struct Pwm {
}

impl Pwm {
    pub async fn new() -> Result<Self, apigpio::Error> {
        // --------- \\

        // TODO: Replace unwrap with error handling `here

        Ok(Self {  })
    }

    pub fn setup(&self) {}
}
